<odoo>

  <!-- Lista IZQUIERDA: Operaciones disponibles -->
  <record id="view_work_queue_item_backlog_list" model="ir.ui.view">
    <field name="name">work.queue.item.backlog.list</field>
    <field name="model">work.queue.item</field>
    <field name="arch" type="xml">
      <list create="0" delete="0" editable="false">
        <field name="workorder_id" string="Operación"/>
        <field name="workcenter_id" string="Centro de trabajo"/>
        <field name="product_id" string="Producto"/>
        <field name="production_id" string="OF"/>
        <field name="state" string="Estado"/>
        <button name="action_assign_to_employee"
                type="object"
                string="→"
                class="btn-primary"
                title="Asignar a la cola del empleado"/>
      </list>
    </field>
  </record>

  <!-- Lista DERECHA: Cola del empleado con reorden -->
  <record id="view_work_queue_item_queue_list" model="ir.ui.view">
    <field name="name">work.queue.item.queue.list</field>
    <field name="model">work.queue.item</field>
    <field name="arch" type="xml">
      <list create="0" delete="0" editable="bottom">
        <field name="sequence" widget="handle" string="#"/>
        <field name="workorder_id" string="Operación"/>
        <field name="workcenter_id" string="Centro de trabajo"/>
        <field name="product_id" string="Producto"/>
        <field name="production_id" string="OF"/>
        <field name="state" string="Estado"/>
        <button name="action_unassign"
                type="object"
                string="←"
                class="btn-secondary"
                title="Quitar de la cola"/>
      </list>
    </field>
  </record>

</odoo>
